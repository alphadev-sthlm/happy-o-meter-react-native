language: objective-c

before_install:
  - npm install -g npm@latest node@latest
  - brew update
 
install:
  - brew reinstall gradle flow watchman xctool
 
branches:
  only:
    - master

script:
- |
  cd HappyOMeter/ios
  npm install
  flow check
  npm test



